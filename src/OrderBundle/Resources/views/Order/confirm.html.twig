{% extends 'base.html.twig' %}

{% block title %}Order confirm{% endblock %}

{% trans_default_domain 'OrderBundle' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-6 col-md-offset-3">
                <h2 class="text-center">{{ 'order.header'|trans }}</h2>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}